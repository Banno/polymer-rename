'use strict';

class BaseExpression {
  /**
   * @param {number} start
   * @param {number} end
   */
  constructor(start, end) {
    this.start = start;
    this.end = end;
  }
}

module.exports = BaseExpression;
